


# root_path = "C:\\Users\\1413d\\Desktop\\lilith\\tests\\simplejson-master"
# tree_root = build_tree_recursive(root_path)

# for item in iterate_code_tree(tree_root):
#     print(item)

# print(export_code_tree_as_json(tree_root, format=str))
# print(stringify_code_tree(tree_root))

# exporter = JsonExporter(indent=2, sort_keys=True)
# print(exporter.export(tree_root))

# print(tree_root.children[8].get_content())
